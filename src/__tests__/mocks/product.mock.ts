import { Product } from '../../entities/product.entity';

export const mockProduct = {
    id: 1,
    name: 'product',
    price: 10,
    createdAt: new Date(),
    updatedAt: new Date(),
} as Product;
